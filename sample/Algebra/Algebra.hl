#?(let ((mods '#!(Classes Poly Complex Ratio)))
    (eval `#!(defpackage Algebra
               (:shadow Prelude pure (Num :all) (Fractional :all))
               (:use ,@#?mods)
               (:export ,@#?(mapcar (curry #'list :|m|) mods)))))
