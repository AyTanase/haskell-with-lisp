(defpackage Regex
  (:use Common Parse Exec)
  (:export (module Regex) Op (module Parse) (module Exec)))

(type regex (=> (Num a) (-> String String (Maybe (tuple a a)))))
(define regex (compose match parse))
