(defmodule Regex ((module Regex) (NFA ()) (module Parse) (module Exec)))

(import Common)
(import Parse)
(import Exec)

(type regex (=> (Num a) (-> (list Char) (list Char) (Maybe (tuple a a)))))
(define regex (compose match parse))
